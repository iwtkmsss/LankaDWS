{
  "name": "bertcrm",
  "version": "0.1.0",
  "private": true,
  "description": "BertCRM development workspace",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,green \"npm run dev --workspace frontend\" \"npm run start:dev --workspace backend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run start:dev --workspace backend",
    "build": "npm run build --workspaces",
    "build:frontend": "npm run build --workspace frontend",
    "build:backend": "npm run build --workspace backend",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspace backend",
    "format": "npm run format --workspace backend",
    "prisma:generate": "npm run prisma:generate --workspace backend",
    "prisma:migrate": "npm run prisma:migrate --workspace backend",
    "prisma:deploy": "npm run prisma:deploy --workspace backend",
    "prisma:studio": "npm run prisma:studio --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
